# BRIEF — {{TASK_TITLE}}

<!-- Self-contained context pack. The reader (Fable 5) will NOT open the repo.
     Inline everything it needs. Written by the prep session (cheap model). -->

## Goal
<!-- One paragraph: what exists at the end that doesn't exist now. -->

## Constraints
<!-- Stack, style rules, platform law (e.g. RTL, tokens-only, dark-only),
     performance budgets, things that must not change. -->

## Acceptance criteria
<!-- Checkable statements. These become the plan's definition of done. -->

## Prior decisions that bind this work
<!-- From vault/memory/docs — so the plan doesn't relitigate settled things. -->

## Repo context (inlined)
<!-- For every file the plan will touch or must respect:
     ### `path/to/file.ts` (role in one line)
     ```ts
     [the relevant excerpt — enough to reason about, not the whole file]
     ```
     Include data shapes, API routes, DB tables involved. -->

## Known risks / traps

## Out of scope
