# COST — {{TASK_SLUG}}

| date | phase | what | estimated | actual | notes |
|---|---|---|---|---|---|

<!-- prep appends the estimate row; the owner's /cost paste fills actual for
     the planning session; fable-call.sh auto-appends escalation rows.
     Third-party authoring APIs (Grok etc.) get one row per pilot + one per
     wave/batch — record measured token counts, not guesses. -->
