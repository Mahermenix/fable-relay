# Story NN — <title>

<!-- One self-contained unit of execution. The executor (a cheap model, e.g.
     GLM via opencode) implements this with ZERO repo exploration beyond what
     this file contains. If it has to go looking, the story is incomplete —
     inline what it needs. -->

## Goal
<!-- One or two sentences: what exists at the end of this story that didn't
     exist before. -->

## Files to touch
<!-- Exact paths. New files marked (new). -->

## Current code context
<!-- Inlined excerpts of the code AS IT IS NOW — enough to implement against
     without opening the repo. Delegate extraction to a cheap subagent if the
     brief doesn't already contain it. Include data shapes, signatures, the
     exact lines that change. -->

## Implementation notes
<!-- Precise change description. A pseudo-diff where it helps. Name the
     specific functions/constants involved. State the non-obvious decisions so
     the executor doesn't have to make them. -->

## Acceptance criteria
- [ ] <checkable statement>
- [ ] <checkable statement>

## Verification commands
<!-- Exact commands with their expected outcomes, so the executor (and the
     reviewer) can self-check. e.g. `npm test -- foo.test.ts  # all green` -->

## Out of scope
<!-- What this story deliberately does NOT do — so scope doesn't creep. -->
